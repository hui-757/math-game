# 环境变量模板

> **复制为 .env 后填入真实值。**
> 
> **注意：.env 文件含密钥，不要提交到 Git！**

```env
# Supabase 项目 URL
# 获取地址：Supabase Dashboard → Project Settings → API → URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key（公开权限）
# 获取地址：Supabase Dashboard → Project Settings → API → Project API keys → anon public
VITE_SUPABASE_KEY=your-anon-key-here
```

---

## 获取步骤（图文）

1. 打开 https://supabase.com/dashboard
2. 选择项目 `jajxhtlwxphlpevmxewk`
3. 左侧菜单 → `Project Settings`（齿轮图标）
4. 顶部标签 → `API`
5. 找到 `Project URL` → 复制 `URL`
6. 找到 `Project API keys` → 复制 `anon public` 那行的值
7. 粘贴到 `.env` 文件对应位置

---

## 安全提醒

| ❌ 不要做 | ✅ 要做 |
|-----------|--------|
| 把 `.env` 提交到 Git | 把 `.env` 加入 `.gitignore` |
| 在代码里硬编码 key | 用 `import.meta.env.VITE_SUPABASE_KEY` 读取 |
| 把 key 发给不信任的人 | 只在 Supabase Dashboard 里管理 |

---

## 当前项目使用的真实值

```env
VITE_SUPABASE_URL=https://jajxhtlwxphlpevmxewk.supabase.co
VITE_SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**key 已存在于：** `D:\Dekstop\practice\game\math-game\.env`
